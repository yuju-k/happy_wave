import 'package:flutter/material.dart';

class TemporaryComponent extends StatelessWidget {
  const TemporaryComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('임시 컴포넌트', style: TextStyle(fontSize: 18)));
  }
}
