import 'package:flutter/material.dart';

/// 🎨 기본 색상 정의
const Color primaryColor = Color(0xFF44C2D0); // 버튼, 강조 색상
const Color textColor = Color(0xFF05638A); // 일반 텍스트 색상
const Color backgroundColor = Color(0xFFD8F3F1); // 입력 폼 배경

/// 📐 공통 스타일 사이즈
const double containerWidth = 350.0; // 폼 너비
const double borderRadius = 20.0; // 버튼/텍스트필드 라운드 정도

/// 🔤 텍스트 스타일
const TextStyle headingTextStyle = TextStyle(
  fontSize: 24,
  fontWeight: FontWeight.bold,
  color: textColor,
);

const TextStyle bodyTextStyle = TextStyle(fontSize: 16, color: textColor);
